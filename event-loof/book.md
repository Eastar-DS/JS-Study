## 코어 자바스크립트 — 읽어야 할 부분

### 1단계: JS 런타임 환경 (싱글 스레드, Call Stack, 실행 컨텍스트, Heap)

- **02장: 실행 컨텍스트** (전체)
    - 2-1 실행 컨텍스트란? — Call Stack과 실행 컨텍스트의 관계
    - 2-2 VariableEnvironment / 2-3 LexicalEnvironment — 실행 컨텍스트 내부 구조

### 3단계: 비동기 패턴 (Callback, Promise, async/await)

- **04장: 콜백 함수** (전체)
    - 4-1 콜백 함수란? — 콜백의 기본 개념
    - 4-2 제어권 (호출 시점, 인자, this) — 콜백의 제어권 이전 개념
    - **4-5 콜백 지옥과 비동기 제어** — Callback Hell과 Promise/Generator/async-await 소개

### 보조 (클로저, this 바인딩 이해)

- **03장: this** — 콜백 내부의 this 동작 이해에 필수
- **05장: 클로저** — 비동기 콜백에서 외부 변수 캡처 원리

---

## 모던 자바스크립트 Deep Dive — 읽어야 할 부분

### 1단계: JS 런타임 환경

- **23장: 실행 컨텍스트** (전체) — Call Stack, 실행 컨텍스트 스택, 렉시컬 환경
    - 23.4 실행 컨텍스트 스택 — **Call Stack 동작 원리의 핵심**
    - 23.6 실행 컨텍스트의 생성과 식별자 검색 과정 — 함수 호출 시 스택이 쌓이고 빠지는 과정을 단계별로 설명

### 2단계: 이벤트 루프 메커니즘

- **42장: 비동기 프로그래밍** (전체) — **이벤트 루프의 핵심 챕터**
    - 42.1 동기 처리와 비동기 처리 — 싱글 스레드, 블로킹/논블로킹
    - **42.2 이벤트 루프와 태스크 큐** — Event Loop, Task Queue, Call Stack, Heap, Web API의 관계

### 3단계: 비동기 패턴

- **41장: 타이머** — setTimeout/setInterval (Macrotask의 대표 API)
    - 41.3 디바운스와 스로틀 — 타이머 활용 패턴
- **45장: 프로미스** (전체)
    - 45.1 콜백 패턴의 단점 — 콜백 헬, 에러 처리 한계
    - 45.2~45.6 프로미스 생성/체이닝/정적 메서드
    - **45.7 마이크로태스크 큐** — **Microtask Queue를 직접 다루는 유일한 절. 반드시 읽어야 함**
- **46장: 제너레이터와 async/await**
    - 46.6 async/await — async 함수, await 키워드, 에러 처리

### 4단계: 실행 순서 예측

- 42장 + 45.7장을 읽은 뒤, setTimeout(fn, 0) vs Promise.resolve().then() 실행 순서를 코드로 직접 확인

### 5단계: 브라우저 렌더링과의 관계

- **38장: 브라우저의 렌더링 과정** (전체)
    - 38.3~38.5 — HTML/CSS 파싱, 렌더 트리 생성 (렌더링 파이프라인)
    - 38.6 자바스크립트 파싱과 실행 — JS가 렌더링을 블로킹하는 이유
    - 38.7 리플로우와 리페인트 — Layout, Paint 단계
    - **38.9 script 태그의 async/defer** — 스크립트 로딩과 실행 타이밍

### 보조 챕터

- **12장: 함수** (12.7.4 콜백 함수) — 콜백 함수 기본 개념
- **40장: 이벤트** (40.1 이벤트 드리븐 프로그래밍, 40.6 이벤트 전파) — DOM 이벤트와 이벤트 루프의 연결

---

## 추천 읽기 순서

| 순서 | 모던 JS Deep Dive | 코어 JS | 키워드 단계 |
| --- | --- | --- | --- |
| 1 | 23장 실행 컨텍스트 | 02장 실행 컨텍스트 | 1단계 |
| 2 | **42장 비동기 프로그래밍** | - | 2단계 |
| 3 | 12.7.4 콜백 함수 | **04장 콜백 함수** | 3단계 |
| 4 | **45장 프로미스** (특히 45.7) | - | 2~4단계 |
| 5 | 41장 타이머 | - | 4단계 |
| 6 | 46.6 async/await | 4-5 콜백지옥과 비동기제어 | 3단계 |
| 7 | **38장 브라우저 렌더링 과정** | - | 5단계 |
| 8 | 40장 이벤트 (40.1, 40.6) | - | 보조 |

**가장 중요한 3개 챕터**: 모던 JS 42장(이벤트 루프), 45.7절(마이크로태스크 큐), 38장(브라우저 렌더링). 이 세 부분이 keyword.md의 2단계~5단계를 직접적으로 다룹니다.

코어 자바스크립트는 이벤트 루프 자체를 다루는 챕터가 없지만, **실행 컨텍스트(02장)와 콜백 함수(04장)**가 Call Stack과 비동기의 기초를 잡아주므로 먼저 읽으면 좋습니다.