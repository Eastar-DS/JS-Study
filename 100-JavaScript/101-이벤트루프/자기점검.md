자기 점검 질문

JS 엔진(V8)
  ├── Call Stack ← 실행 컨텍스트가 쌓이는 곳
  └── Heap      ← 객체/함수가 저장되는 곳

런타임 환경 (브라우저 or Node.js)
  ├── JS 엔진
  ├── Web APIs (setTimeout, fetch, DOM 등)
  ├── Task Queue / Microtask Queue
  └── Event Loop ← 2단계에서 본격적으로
"자바스크립트는 싱글 스레드"라는 말이 정확히 무엇이 싱글 스레드라는 건지?
함수를 호출하면 Call Stack에 무엇이 쌓이는지? (실행 컨텍스트)
그 실행 컨텍스트 안에는 어떤 정보가 들어있는지?
setTimeout의 콜백 함수 객체는 Heap에 저장되는데, 타이머 카운트다운은 어디서 처리되는지?
V8 엔진은 setTimeout을 모르는데, 어떻게 실행할 수 있는지?
이 5개 질문에 막힘 없이 답할 수 있다면, 1단계는 확실히 끝난 것이고 **2단계(이벤트 루프 메커니즘)**로 넘어가면 됩니다.

